# 工程完成计划（v2 · 含工时估算与分批方案）

> 2026-09-16 · 讨论稿。第 1 章编号重排已完成（见 `CHANGELOG.md` v0.7.4）。
> 现状：**10 / 114 节已建成**，九套校验对已建内容全绿。

---

## 一、总账：还有多少活

| 口径 | 数量 |
|---|---|
| 同济骨架 | **12 章 / 80 节** |
| 托马斯独有内容 | **34 节**（其中 8 节是航发方向硬需求） |
| 并集合计 | **114 节** |
| 已建成 | 10 节（第 1 章） |
| **待建** | **104 节** |

## 二、工时的实测依据（不猜，用第 1 章的真实数据）

第 1 章 10 节已建成，可直接量出"一节"的体量：

| 指标 | 第 1 章实测 | 折算每节 |
|---|---|---|
| 中文正文 | 65,120 字符 | **6,512 字符/节** |
| 英文正文（等权对照） | 101,224 字符 | 10,122 字符/节 |
| 例题 | 31 道 | 3.1 道/节 |
| 测验题 | 78 道 | 7.8 道/节 |
| 文件行数 | 4,114 行 | 411 行/节 |

**按此推算剩余 104 节的规模 ≈ 中文 68 万字符 + 英文 105 万字符，约 4.3 万行 HTML。**

> 后 11 章的难度更高：第 8–12 章（空间解析几何、多重积分、曲线曲面积分、无穷级数）
> 配图需求与考研题占比都更高，**单节成本约为第 1 章的 1.2–1.5 倍**。

## 三、工时估算（以"批次"为单位，不承诺日历时间）

我按"一轮 = 一次可交付批次（含写作 → 组装 → 跑全部校验 → 修到全绿）"来计量。
**每轮的实际时长取决于算力与并行度**，所以我给的是**轮次**与**并行策略**，你可以按自己的节奏定。

| 批次 | 范围 | 节数 | 难度系数 | 预计轮次 |
|---|---|---|---|---|
| **R0** | 第 1 章改造：`.link-box` 融入正文 + 去 AI 味改写 + 补 9 张图 | 10 节（改） | — | **1–2 轮** |
| **R1** | 第 2 章 5 节 + 托马斯补充一 2 节 | 7 | 1.0 | **1 轮** |
| **R2** | 第 3、4、5 章 | 18 | 1.0 | **2 轮** |
| **R3** | 第 6、7 章 | 11 | 1.1 | **1–2 轮** |
| **R4** | 第 8、9 章（空间几何 + 多元微分，配图最重） | 16 | **1.4** | **2–3 轮** |
| **R5** | 第 10、11 章（重积分 + 曲线曲面积分） | 12 | 1.3 | **2 轮** |
| **R6** | 第 12 章（无穷级数）+ 托马斯补充其余 32 节 | 40 | 1.2 | **3–4 轮** |
| **R7** | 收尾：`index.html` / `exam.html` / `cheatsheet.html` / `site.test.js` 定稿 | — | — | **1–2 轮** |
| | **合计** | **104 节** | | **13–19 轮** |

**参考点：第 1 章那 10 节，上一任 agent 是在一次上下文用尽之前完成的**
（据 `交接说明_v0.7.2.md` 自述"上任 agent 的上下文已用尽"）。
这说明**单轮大约能产出 8–12 节**——上表的轮次估算就是按这个吞吐量算的。

## 四、并行策略（关键：这是能把总时间压下来的唯一杠杆）

交接包里的 `可复用提示词集.md` **M2 模块**就是为这件事写的，并且**已经在本工程实战过**
（"12 份 dispatch 实战"）。要点：

1. **先有规范再派活**：`写作规范.md` + `tools/section-format.md` 就是那份"唯一事实来源"。
   派发时**让执行者自己 read 规范文件**，不要把规范贴进 prompt（避免版本漂移）。
2. **每份任务书 = 范围（精确到节 id）+ 逐节必覆盖知识点 + 深度要求 + 硬约束 + 交付格式**。
3. **任务书里最有效的三句话**（实测）：①"数学必须正确，每道例题都自己验算一遍，这是最重要的一条"；
   ②"写完回报你在验算中发现的任何不确定之处"；③"文件已写完就直接改，别重写整个文件"。
4. **中途改需求要发增量补丁**，并明确"我上一条说的 X 作废，改为 Y"。

**具体到本工程**：一批里的各节相互独立，可以**并行 5–6 路**写，每路交一个
`/tmp/sec/<sid>.html` 片段，我再统一组装、跑九套校验、逐节抽查。
风险是文风会散——用 `s1-1` 当样板 + 组装后统一过一遍来压。

## 五、每轮的验收口径（已实测可跑）

```bash
cd 高等数学学习站
python3 tests/check-structure.py && python3 tests/check-bilingual.py && python3 tests/check-html.py
python3 tools/check-numbering.py && python3 tools/check-refs.py
cd tests && node quiz.test.js && node review.test.js && node videos.test.js && node theme.test.js
node check-layout.js && node check-contrast.js
```

**判定**：`check-bilingual` 必须 exit 0；`check-structure` / `check-html` 只允许报"未建项"；
4 个 DOM 测试 0 失败；`check-numbering` 全 ✓；`check-layout` / `check-contrast` 不得出现新的溢出或 <4.5:1。
**每轮结束必须解压到全新目录再跑一遍**（上一任 agent 就是靠这一步抓到 320px 下控件溢出 142px）。

## 六、建议的分批方案（供你选）

| 选项 | 做法 | 适合 |
|---|---|---|
| **A · 稳扎稳打** | 严格按 R0→R7 顺序，每轮全绿才进下一轮 | 你想随时能停下来验收 |
| **B · 先通后精** | 先把 104 节的"骨架 + 定义/定理/方法 + 自测"铺完（内容略简），再统一做文风/配图/例题升级 | 想尽快看到整门课可用 |
| **C · 双线并行** | 主线按 A 推进；同时另开一路补托马斯 36 节（与主线章节解耦） | 想压缩总时长 |

> **我的建议：A + C。** 主线按章节顺序稳推（每轮可验收），托马斯补充单独一路并行，
> 因为它与主线章节不冲突、且 8 节硬需求内容独立成篇。

## 七、需要你拍板的 5 件事

1. **分批选项**：A / B / C（我建议 A+C）
2. **要不要用并行子代理**：一批里 5–6 路并行写，我负责组装与统一文风
3. **第 1 章改造（R0）先做还是先铺新章**：我建议先做 R0（它是文风样板，
   定完样板后面 104 节才有统一参照）
4. **`exam.html` 范围**：全 114 节，还是先做第 1 期 17 节
5. **视频分 P 号**：`s1-4`…`s2-5` 目前 `verified: false`。按现有规则会显示"待核实"提示；
   你若愿意点开 B 站核对一次，我可以批量回填，按钮就不再挂提示
