# 编排与文风规范

> 起因：用户 2026-09-16 一次性提了 8 条意见，其中 5/6/7/8 条是**内容与编排**层面的：
> ① 文字 AI 味太重、详略不分、要点不明确；② 知识关联不该单列成模块；
> ③ 模块编排要主次清晰、循序渐进；④ 必要处要加函数图像（数形结合）。
> 本文是这四条的执行依据。**动笔前先读本文，再读 `写作规范.md`（格式）与 `视觉规范.md`（视觉）。**

---

## 一、模块分层（需求 4/7）

页面要"一眼看出哪里是主干"。三层，用**视觉比重**而不是标题字号来表达：

| 层 | 模块 | 处理 |
|---|---|---|
| **第一层（主干）** | `.insight-box` 直观引入 · `.def-box` 定义 · `.thm-box` 定理 · `.method-box` 方法 · `.example` 例题 · `.quiz` 习题 | 纸面正文 + 4px 语义色条 + **整条标题色带**；字号 1rem；例题/习题给足留白 |
| **第二层（辅助）** | `.warn-box` 易错点 · `.exam-box` 考察要点 · `.q-review` 讲评 | 保留色条与色带，但**去掉外描边**、字号略小（`.86~.95rem`） |
| **第三层（附录）** | `.app-box` 工程联系 · `.map-note` 教材映射 · `.link-box` 知识关联 | **折叠成 `<details>`，默认收起**；标题无底色小字（`.82rem`）；正文 `.88rem` 且用次要色 |

**为什么第三层要折叠**：用户原话是"不是主干内容的部分如工程联系、教材映射写的比重太大而繁杂，
视觉比重太重"。折叠是唯一能同时做到"内容还在"与"不抢注意力"的手段。
**默认收起**，需要时点开——所以第三层的**标题必须能独立成立**（别写成"详见下文"）。

### 阅读动线（每节固定顺序）

```
h2（含学时徽章）
└ 承上启下两段：前置知识 / 本节要解决的问题   ← 第 6 条：把"承上启下"写在这里（见 §二）
└ 💡 直观引入（insight-box）                 ← 先给一张图或一个具体例子
└ 📘 定义（逐个，含图）
└ 📐 定理 / 判定（含适用条件 + 直观含义）
└ ⚙️ 方法（步骤化）
└ 例 1（基础）→ 例 2（中等）→ 例 3（考研）    ← 例题是主干，给足空间，不跳步
└ ⚠️ 易错点（辅助层）
└ 🎯 考察要点（辅助层）
└ 自测（7 题 + 讲评）
└ ▸ 折叠：工程联系 / 教材映射 / 知识关联（附录层）
└ 视频入口（顶部已有一个，见 §五）
```

**循序渐进的三条硬要求**

1. **概念必须"先有动机再给定义"**：每个定义前必须能回答"没有它，上一段哪里卡住了"。
2. **例题必须成梯度**：基础 → 中等 → 考研，且**后一道题至少用到前一道题的一个结论或方法**。
3. **不许跳段**：某节确实没有"定理"或"方法"，就地说明理由（"本节只给定义与判定，方法留到 §x"），
   而不是悄悄省掉——读者会以为自己看漏了。

---

## 二、知识关联不再单列（需求 6）

**规则：取消独立的 `.link-box` 模块**。它与正文分离，读者读到那里已经忘了上下文。
改为**三处就地融合**：

| 原 `.link-box` 的内容 | 新位置 |
|---|---|
| **承上**（本节依赖前面什么） | 写进「前置知识」段，**一句话点出依赖的是哪个定义/结论**（写成 `由 §1.1 定义 1.4`） |
| **启下**（本节结论后面在哪用） | 写进「本节要解决的问题」段的**最后一句**（写成 `这套记号在 1.3 节讲函数极限时立刻要用`） |
| **横向**（与其它概念是同一件事） | 写在**对应概念旁边**的一句话里，例如讲复合函数时直接说"这与 §1.6 的换元是同一件事" |

**判据**：一节的正文里应当能数出**至少 3 处**"回指"或"前瞻"的句子（带 §号）。
如果只能在末尾的关联块里看到这些，就是没做到融合。

> 当前状态：`ch1.html` 的 10 节仍是独立 `.link-box`（已折叠降权）。
> **下一任 agent 的任务**：把这 10 个块的内容按上表融进正文，然后删掉 `.link-box` 模块。

---

## 三、文风：去 AI 味、详略有别、要点前置（需求 5）

环境里**没有写作类 skill**（本会话只有两个 Cordis 技能，用户级 `~/.dsh/skills` 不存在），
所以这套规则由本文承载。**逐条对照着改，不要凭感觉润色。**

### 3.0 总要求：**严谨、简练、精干**（用户 2026-09-16 原话，优先级高于以下各条）

> 「严谨、简练、精干：表述以事实和结论为主，用词准确，不夸张、不抒情；
> 能一句说清不写两句，删净不承担信息的修饰语与重复强调；不堆砌套话与排比，段落不宜过长。」

四条可执行的判据：

| # | 要求 | 反例（改前） | 正例（改后） |
|---|---|---|---|
| 1 | **以事实和结论为主** | "这两个概念马上就有大用" | "这两个概念是第 2 章求导法则的前提" |
| 2 | **不夸张、不抒情** | "求导会全面崩盘" / "本节最要命的逻辑错误" | 直接删掉；或改"本节主要的逻辑错误" |
| 3 | **一句说清不写两句** | "有一件事要在本节就立住：……" | 删掉这句引导语，直陈事实 |
| 4 | **删净不承担信息的修饰语** | "务必" / "千万" / "顺手" / "很实在" / "天生就是" | "要" / "不要" / "直接" / "很具体" / "本身就是" |

**机器检查**：`python3 tools/check-style.py` 的 **①-b「文风违规」** 一节守着这一条
（词表在 `tools/check-style.py` 的 `BANNED_STYLE`）。**命中即改，不得放过。**

### 3.1 要点前置

每个内容块的**第一句必须是结论或问题**，后面才是解释。不要"由浅入深地铺垫到第三句才给答案"。

| ✗ AI 味写法 | ✓ 人话写法 |
|---|---|
| "在高等数学的学习过程中，我们首先需要明确……" | "定义域是函数的第一个身份。求它只有五条限制。" |
| "值得注意的是，该式蕴含了……" | "这个式子说的是：分母不能为零。" |
| "综上所述，我们可以得到如下结论" | 直接给结论，不加引导语 |
| "本文将深入探讨……" | "本节解决一个问题：……" |

### 3.2 详略有别（这是用户最主要的抱怨："详细和简要的区分不明确"）

| 内容 | 篇幅 | 判据 |
|---|---|---|
| 定义 / 定理陈述 | **详**，逐字可默写，含全部条件与限定语 | 删掉任何一个条件，命题就不成立 |
| 直观引入（insight-box） | **简**，2–4 句 | 能在一屏内读完 |
| 定理的直观含义 | **简**，1–2 句 | 说"它在几何上是什么" |
| 例题解答 | **详**，一步一行，行内写理由 | 每一步都能回答"为什么可以这样" |
| 易错点 | **简**，每条 1–2 句 | 只写"错在哪 + 正确是什么" |
| 考察要点 | **简**，每条 1 句 | 只写"考什么 + 看到题该想什么" |
| 工程联系 | **最简**，≤2 句，且折叠 | 只留"这件事在工程上叫什么" |
| 教材映射 | **最简**，1–2 句，且折叠 | 只留"英文书在哪一节" |

### 3.3 禁用清单（AI 味的具体来源，改文时全文搜一遍）

```
值得注意的是 · 不难发现 · 众所周知 · 综上所述 · 首先/其次/最后（连用三次以上）
深入探讨 · 全方位 · 赋能 · 助力 · 旨在 · 以期 · 值得注意的是 · 在一定程度上
需要指出的是 · 我们可以看到 · 让我们 · 接下来我们将 · 本小节将
```

### 3.4 句子与段落

- **一段一件事。** 超过 4 行的段落拆开。
- **用"你"**（读者是大一学生）："你会看到"比"可以看到"更像人话。
- **公式前后说清"这个式子说的是什么"**，不要只甩公式。例：
  `$b=\frac a2$ 这个结果的意思是：连续性只能把一个参数与另一个挂钩，它锁不住两个参数。`
- **不要用序号词作结构**（"第一点/第二点"），靠段落顺序与加粗要点体现层次。
- **加粗只给"要点"，不给整句。** 一段里加粗不超过 2 处。

### 3.5 自检（改完每节必须做）

1. 全文搜一遍 §3.3 的禁用词，命中即改。
2. 数一数：这一节有几处**带 §号**的回指/前瞻？少于 3 处说明"融合"没做到（见 §二）。
3. 抽查三个最长的段落：能不能拆？能不能砍掉一半而不丢信息？
4. 把「工程联系」与「教材映射」折起来读一遍正文——主干是否自足？

---

## 四、图解：本工程**不配图解**（2026-09-16 用户决定，原"必须配图"条款作废）

> **这一节是历史记录与禁令，不是施工要求。**
> 2026-09-16 用户决定：**全站删除所有内联 SVG 图解，后续各期也不再新增**。
> 当时的原话是"算了还是不要图解了，修完还是 bug，前面的删掉后面也别加了"。

### 4.1 为什么废除

从 v0.7.8 到 v0.7.28，图解一共出过这些**只有肉眼或算坐标才能发现**的问题：

| 问题 | 例子 |
|---|---|
| 符号语义画错 | 图 8 把"可去间断点"的两支画成**收敛到两个不同高度**；判断不了空心/实心各代表什么 |
| 交点数量与图注不符 | 图 9 的水平线实际与曲线**交于 3 点**，图上只标 2 个 |
| 曲线位置错 | 图 7 两条线在纵轴处被截断，读起来像"交叉换位" |
| 布局互相压叠 | 图 8 的振荡曲线穿过自己的标题；文字被轴线截断 |
| 标记点不在曲线上 | 极值点、切点画在曲线外 |

这些都无法靠静态校验拦住：`check-structure` / `check-bilingual` / `check-html` 全过，
`check-figure.py` 也报"0 可疑点"，但图**就是错的**。
每轮修正都要"截图 → 肉眼 → 再截图"，反复 6 次仍没排干净的记录（v0.7.28）。

**结论：投入产出比不成立。** 数学结论用文字、公式与表格表达已经足够清楚。

### 4.2 后续施工的硬规则

1. **不要新增任何图解**：不写 `<figure class="figure">`、不写内联 `<svg>` 图示、
   也不要用 `.viz[data-viz]`（`assets/js/viz.js` 里的图形是线代站遗留，本工程不用）。
2. **正文里不要出现"如图""见图 N""左图/右图"这类指代**——没有图就没有指代对象。
   凡是原本靠图说明的"变化过程"，改写成**文字描述 + 公式 + 表格**。
3. **不要复活旧图**：`check-figure.py` 与 `tools/inspect-figure.js` 保留（可能还有别处引用），
   但不要再往页面里加图。
4. `tests/site.test.js` 里有一条"零图形"断言（章节页不应有 `.viz[data-viz]`），
   加图会让它失败——这是有意设的护栏。

### 4.3 历史文件（只读，不再维护）

- `tools/check-figure.py`、`tools/inspect-figure.js`、`tools/shot-figure.js`、`tools/add-figures-ch1.py`
- 各章节页里已删除的 16 幅图（第 1 章 9 幅、第 2 章 5 幅、托马斯补充 2 幅）

---

## 四·补、绘图引擎 `assets/js/plot.js` 与配图纪律（2026-09-17 新增）

> 本节与 §四 不冲突：§四 讲的是**一般情况不配图**；本节讲**获准配图时**（目前仅第 8 章）
> 该用什么工具、守什么纪律。

### 1. 用引擎，不要手写折线

`assets/js/plot.js` 提供 `Plot.render(容器, spec)`，spec 是纯数据。它做的事：

| 能力 | 说明 |
|---|---|
| 自适应细分 | 按弦高误差加密，曲率大处自动变密 |
| 间断断开 | 相邻点跨度超阈值即断开，**不会连出并不存在的竖线** |
| 数值比例映射 | x/y 各自比例尺，几何不变形（圆是圆、斜率为真） |
| 隐函数等值线 | marching squares，可画 `x²+y²=1` 这类非函数曲线 |
| z 层级 | 区域 0 → 参考线 1 → 曲线 2 → 标记 3 → 文字 4 |
| autofit | 默认铺满坐标范围，并按 y 范围裁剪 |
| 参数滑块 | spec 里声明 `params` 即生成滑块，拖动实时重绘 |
| 3D 投影 | `Plot.makeProjection()` 返回深度，调用方按深度排序 |

**不要回到手写 `<path d="M…L…">`**：v0.7.26 那批图的问题（两支画到一起、交点数量与图注不符）
根子都在"手算坐标 + 折线连接"。

### 2. 配图纪律（两条硬规矩，一条原则）

1. **坐标不许手填**。凡需要精确落点（切点、交点、极值点、渐近线），
   **先用方程解出坐标，再把数值写进 spec**。v0.7.39 的教训：ε-δ 图里两条 δ 竖线
   我手填成 1.2/3.2，而曲线与 ε 边界的真实交点是 1.5/2.5 —— 图注说"竖带塞进横带"，
   图却自相矛盾。
2. **图内标注要克制**。不必每幅都把"这是什么、为什么、注意什么"全写进图里；
   一句到三句、只标**读图必需**的量（关键点、关键线的含义）。
   解释性文字放 `figcaption`，不放图内。
3. **准确性优先于美观**（用户明确要求）。宁可少标、留白，也不许标错：
   - 标记点必须落在曲线上（用 `--oncurve` 或坐标代入验证）
   - 参考线的位置必须与它声称的含义一致（渐近线就该是渐近线）
   - 交点数量必须与图注一致

### 3. 施工顺序（用户 2026-09-17 明确要求）

> **先把引擎的准确性做完，再画其他章节的图。**（原话："优化完引擎再做其他章节，不然你还要返工"）

理由：图是引擎的输出，引擎若不能保证准确，后面每一幅都要回头重画。
因此顺序固定为：

1. 引擎准确性（见下节 §4）
2. 第 8 章 6 节配图
3. 第 9 章 10 节
4. 其余章节按需配图

### 4. 准确性要"做进引擎"，不靠自觉

用户强调：**"图表内容克制，最重要的要求是画图准确。"**
所以准确性不能只写在文档里靠人守，要在引擎层面强制：

| 机制 | 做法 | 拦截什么 |
|---|---|---|
| **点必须可真在曲线上** | 声明 `at: [x, y]` 时，若该点关联了曲线表达式，引擎用表达式算出 `y` 并**断言与声明值一致**（容差内），不一致就报错 | 标记点画在曲线外（v0.7.26 踩过） |
| **参考线必须名副其实** | `role:'guide'` 的线若声明了 `asymptote`/`tangent`/`level` 等语义，引擎校验：渐近线处函数确实发散、切线斜率确实等于导数、水平线高度确实等于声明的值 | "看着像渐近线其实不是" |
| **交点数量要与声明一致** | 声明 `crossings: 2` 时，引擎用数值方法数一遍交点，不符就报错 | 图注说"交于两点"而图上三点（v0.7.26 踩过） |
| **标注克制** | 图内默认只画坐标轴刻度 + 关键点符号；文字标签需显式给出（`label`），且引擎检查文字不越界、不互相压叠 | 图内信息过载 |

**校验失败时渲染直接抛错**，而不是画出一张"看着没问题"的错图。

### 5. 验收：两道关（沿用 §四.3）

- **关一**：`tests/plot.test.js`（引擎自身 31 项）+ 目录页 `check-html` 等静态校验
- **关二**：截图肉眼复核；涉及精确落点的图，必须用坐标计算或 `--oncurve` 复核

---

（已改动，务必按新规则）

⚠️ **用户要求删除讲解区底部的"看完还想听一遍？"卡片**（2026-09-16）。
现在规定：**每个小节只有一个视频入口，位于讲解区顶部**（`data-video-pos="top"`）。
`tests/check-bilingual.py` 会正向断言"没有底部入口"，写回去会报错。

---

## 六、给下一任 agent 的待办（按优先级）

| # | 任务 | 依据 | 规模 |
|---|---|---|---|
| 1 | 把 `ch1.html` 10 节的 `.link-box` 内容**融进正文**，然后删掉该模块 | §二 | 10 节 |
| 2 | 按 §三 改写 10 节的文字（要点前置、详略分层、去 AI 味） | §三 | 10 节，**最长的一项** |
| 3 | 按 §四.1 补齐 9 个缺图（`s1-2`…`s1-10`） | §四 | 9 张图 |
| 4 | 给 `viz.js` 加高数图形类型（ε-N 窗口、左右极限、单位圆夹逼…），再把交互图接到对应节 | §四.2 | 引擎 + 内容 |
| 5 | 缩短 `.app-box` / `.map-note` 正文（现偏长，折叠后仍嫌繁琐） | §一/§三.2 | 20 个块 |
| 6 | 第 2 章（`ch2.html` 5 节）与托马斯补充（`supp1.html` 2 节） | 第 1 期计划 | 7 节 |
| 7 | `index.html` / `exam.html` / `cheatsheet.html` 的收尾；`site.test.js` 对齐高数站口径 | README §八 | — |

**动笔前请先读**：`写作规范.md`（格式与双语）→ `视觉规范.md`（视觉与对比度）→ 本文（编排与文风）。

---

## 四·补二、VISUAL_RULES（作图观感硬要求，每次图像修复后追加）

### 2026-09-17（用户对图 8/9 的观感反馈）

| 问题（从截图提取） | 根因 | 修法（已落地） |
|---|---|---|
| 振荡图中心糊成实心黑块 | `seq` 的连线用**逐点 `line` 段**，密集处叠加成块 | 连线改为**一条 `path`**（更细、连续），并暴露 `linkWidth` / `linkOpacity` |
| 振荡区被压窄、看不清 | 取景 `[-1,1]` 过宽 | 收窄到 `[-0.55,0.55]`；中心密集是 $\sin(1/x)$ 的真实性质，取景决定可读性 |
| 跳跃图左下大片留白 | `yr` 过宽 | `[-1.6,1.6]` → `[-1.35,1.35]` |
| 子图标题与曲线相碰 | 子图默认内边距不足 | 子图 `padding` 的 `t` 至少 24；必要时单图覆盖 |
| 标签互相压叠 | 标签位置手填 | 标签坐标逐个核对，避开曲线与其它标签 |

### 沉淀为通用要求

**布局**
- 侧栏收起后正文居中（差 ≤ 2px），不挤跑、不残留偏移。
- 无异常横向滚动条；长公式/表格只允许局部滚动。
- 顶栏、侧栏、底栏稳定，不因切页/弹窗消失。

**构图（本次新增）**
- 子图必须有**边框 + 不外溢**：溢出的坐标轴会糊到邻图，用 `clipPath`（`setAttributeNS` 设 id）
  并叠加**白底遮罩**兜底。
- 子图标题画在**父级**（画在子图内会被裁剪）。
- 取景范围由**要看清的特征**决定，不由数据边界决定。
- 曲线密集处必须用 `path` 连续绘制，禁止逐点折线段叠加。
- 子图文字不放大（`spec.__sub` 时 `TS` 不随宽度增长），否则子图字比父图还大。

**准确性（沿用 §四·补 第 4 节）**
- 标记点必须过 `onCurve` 校验；数列必须声明 `monotone`；
- 参考线必须声明 `level`；交点数量必须与图注一致。

**回归**
- 任何图像修复不得回退已通过模块，不得改坏正确文字；修完必须重跑
  `check-html` + `plot.test.js` + `check-figure`，并对修改过的图截图复核。

### 2026-09-17（文件更新 / 快捷方式 / 废弃清理）

**废弃清理的门禁（本次执行）**
- 先 dry-run 出候选清单，每项必须有「引用搜索结果」作证据。
- 「确定废弃」必须同时满足：页面 / JS / CSS / 数据源 / i18n / 测试 / 构建配置**全部无引用**。
- 删除前备份到 `.backup/废弃清理-<时间戳>/`，并写 `manifest.md`（原路径 / 备份路径 / 原因 / 证据 / 回滚命令）。
- 删除后**再次搜索引用**，并跑全套校验 + 测试。
- **文档里被引用的工具不算废弃**（如 `inspect-figure.js`、`shot-figure.js`），保留。

**本次经验**
- 删模块会连带失效「专测该模块的测试段」——必须一并处理，否则测试报 `ENOENT` / 断言失败。
- `safe_edit` 的长度自检在**有意删除**时会误拦（改后 76%）；此时应显式传 `min_ratio=None`，
  而不是调低全局阈值——保留备份与写后复核即可。
- 删除后 `grep` 仍有残留时，要先判断残留是「真引用」还是「文档/注释里的历史提及」。

**桌面快捷方式（macOS）**
- 用 `.command` + `chmod +x`，工作目录设为项目根，目标写**相对路径** `index.html`。
- 同名先重命名备份，不覆盖。
- 创建后必须**实际执行一次**验证可启动。

### 2026-09-17（用户第二次指出图 8/9 仍不合格）

**最重要的一条教训（比坐标写错更严重）**

> 我在图 9 的三个交点上写了 `tol: 0.01`，而引擎把 `e.tol` 当作**准确性校验的容差**，
> 于是偏离曲线 0.997 的点被判为"校验通过"——**等于我自己放水绕过了校验机制**。

**修法（已落地，属引擎硬约束）**：`verify()` 的容差**强制上限 1e-3**
（`Math.min(e.tol || 1e-6, 1e-3)`），spec 再也无法放宽校验。

**坐标必须解方程，不能"看着像"**

图 9 的三个交点我填了 −1.879 / 0.347 / 1.532，实际那是 $f(x)=0$ 的根；
图里画的水平线是 $y=1$，正确解是 $x=-\sqrt3,\,0,\,\sqrt3$。**两步都要做**：
①解方程得到精确坐标；②写进 spec 后由 `onCurve` 严格校验兜底。

**奇点振荡的采样（$\sin(1/x)$）**

| 做法 | 结果 |
|---|---|
| 沿 x 均匀采样 | 中心必然混叠成**竖线团**（频率趋于无穷，加密解决不了） |
| 纯几何间距采样 | 极易取错方向、点全挤到一处（我踩过，整条曲线被裁没） |
| **均匀覆盖 + 奇点近旁按零点 $x=\pm1/(k\pi/2)$ 取样** | ✓ 两侧波形清楚，中心呈现"越来越密"的真实形态 |

**输出前必须做的两件事**
1. 交叠/重叠类缺陷：改完**必须重新截图**（我曾对着未更新的缓存图判断"已修好"）。
2. 路径类缺陷：检查 `d` 是否**真的覆盖了整个绘图区**（我曾出现 `d` 全长 19613 但点全在同一处）。

### 2026-09-17（用户第三次指出振荡图仍不合格 → 换图）

**核心教训：选函数时要先想"这个函数画得出来吗"**

我连续两轮试图把 $\sin\dfrac{1}{x}$ 画干净（先改连线为 path、再改采样方式），**都没成功**。
原因是数学上的：该函数在 0 附近**频率无界**，任何有限采样都必然混叠成竖线团。
—— 这不是调参能解决的问题，**是选题问题**。

**改用的例子**：$x\sin\dfrac{1}{x}$（$x\ne0$），$f(0)=0$。
- 同样在 0 处**无限次振荡、极限不存在** → 教学要点完全保留；
- 但**振幅随 $|x|$ 收敛**，曲线夹在 $y=\pm|x|$ 两条包络线之间 → **有限采样就能画干净**。

**沉淀为作图前置检查（作图前必答三个问题）**
1. 这个函数在图上**有界可画**吗？（振幅是否收敛、是否有频率奇点）
2. 若必须展示"无界/无限振荡"，**是否可以用包络线或密度带表达**，而不是硬画每一条波？
3. 图与正文举的例子**是否一致**？若不一致，**必须在图注或正文里说明为何换例**。

**流程教训**
- 图与文案举例不同时，**中英两侧都要补说明**（否则双语段数或语义不对齐）。
- 判断"改好了"之前**必须重新截图**，并检查路径是否真的覆盖绘图区。

### 2026-09-17（检查器本身的教训）

**校验器必须与被检内容的结构一致。**

`check-bilingual` 原先按「整节拼接后取编号集合」比对中英编号，而中英段数本就不等
（中文有独占的题干与注释），于是配对错位、把无关段落放在一起比——**误报 165 处**。
改为「逐 `.bi` 容器内成对比较」后降到 **13 处**（减少 92%）。

**沉淀为通用要求**：
1. 双语类检查**一律在 `.bi` 容器内成对比**，禁止整节拼接后比集合。
2. 段数不等时**只比能配上的**，并把"配不上的段"单独登记（而不是制造错位噪音）。
3. 报告必须带**可定位的信息**（小节号 + 中文段开头），否则无法人工核对。
4. 改检查器后要用**已知正确的样本反向验证**（改完当场跑一遍全站点数对比）。
