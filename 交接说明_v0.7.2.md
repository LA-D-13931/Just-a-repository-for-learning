# 交接说明 · 高等数学学习站 v0.7.2

> 给下一个工作区的 AI agent。**先读这一页，再读 §3 的三份规范。**
> 交接时间：2026-09-16 ｜ 上任 agent 的上下文已用尽，以下状态已全部实测过。

---

## 1. 一句话现状

**第 1 章（函数与极限）10 节 + 章末测验已全部建成并通过全部校验**（11 万字符内容、78 道题）；
网站引擎（测验、进度、讲评、主题、语言开关、视频入口、图解）已完整可用。
**第 2 章与托马斯补充仍是占位页**。内容层面的"文风去 AI 味、知识关联融合、补图"只完成了
`s1-1` 的样板（见 §4），**其余 9 节待改**。

---

## 2. 最快上手

```bash
cd 高等数学学习站

# 零依赖的三个结构校验（随时可跑）
python3 tests/check-structure.py     # 结构 / 双语覆盖 / 视频登记 / 难度分布
python3 tests/check-bilingual.py     # 双语段数 / 公式一致 / 纯中文模式无英文漏出
python3 tests/check-html.py          # 标签配平 / 链接 / 锚点

# 需要依赖的（第一次跑先装：cd tests && npm install）
cd tests && npm install
node quiz.test.js      # 36 项   测验引擎
node review.test.js    # 77 项   讲评报告 / 订正
node videos.test.js    # 37 项   视频入口
node theme.test.js     # 62 项   主题 / 亮暗 / 中英语言开关
node check-layout.js   # 真实浏览器：320/768/1280 溢出 + 暗色 + 公式基线
node check-contrast.js # 真实浏览器：4 主题 × 亮暗 的文字对比度
python3 tools/gen-themes.py   # 重新生成色板（会跑几十条对比度断言）
```

**环境要点（踩过的坑）**

| 事项 | 说明 |
|---|---|
| **本机没有 `bun`** | 原骨架的 `bun.lock` 已删；`.test.js` 一律用 `node` 跑 |
| `~/.npm` 缓存权限异常 | 加 `--cache /tmp/npmcache-dsh` |
| **工程不带 `node_modules`** | 故意删的（50MB）。跑 `.test.js` 前先 `npm install` |
| `check-layout.js` / `check-contrast.js` | 用本机 Microsoft Edge（无 Chrome 也能跑）；MathJax 优先读 `/tmp/mj/tex-svg.js`，没有就联网 |
| 打包 | 用 **Python `zipfile`**（macOS 自带 zip 会让中文名在 Windows 上乱码）；断言 `flag_bits & 0x800` |
| 交付验收 | 必须**解压到全新目录再跑一遍全部校验**——本轮就是靠这一步抓到了 320px 下主题控件溢出 142px |

---

## 3. 动笔前必读的三份规范（顺序不能反）

| # | 文件 | 管什么 |
|---|---|---|
| 1 | `写作规范.md` | **格式**：六段式骨架、可用 CSS 类、`.bi` 双语容器写法、视频登记字段、可用的校验命令 |
| 2 | `视觉规范.md` | **视觉**：正文不铺彩色（语义靠色条+整条标题带）、三层视觉分层、对比度硬指标、踩坑清单 |
| 3 | `编排与文风规范.md` | **编排与文风**：模块三层、阅读动线、**去 AI 味规则与禁用词**、知识关联融合、什么时候必须配图 |

另有：`主题配色表.md`（4 套色板，生成物）、`术语表.md`（中英术语 + 同济↔托马斯叫法差异）、
`对照偏差清单.md`（中英对照的待确认项）、`第1章_视频对照表.md`（视频↔知识点，含来源与时长）。

---

## 4. 用户 8 条意见的完成状态

| # | 用户要求 | 状态 |
|---|---|---|
| 1 | 三套主题拉开区分度 | ✅ 已改：纸面彩度重新定档（霜蓝 0.075 / 暖沙 0.125 / 竹青 0.105 / 墨檀 0.115），并给三套补偿强调色彩度 |
| 2 | 讲评文字顶格子 | ✅ 已修：`.q-review` / 讲评面板 / 测验区补 1.1rem 水平内边距 |
| 3 | 删掉讲解区底部的视频卡片 | ✅ 已删（10 处）；**校验口径已改为"每节只有一个顶部入口"**，写回底部会报错 |
| 4 | 主次不明确、辅助内容比重太重 | ✅ 已做**三层视觉分层**（主干 / 辅助 / 附录）；工程联系、教材映射、知识关联**折叠成 `<details>` 默认收起** |
| 5 | 文字 AI 味重、详略不分、要点不明 | ⚠️ **规则已落文**（`编排与文风规范.md` §三，含禁用词清单与篇幅表）；**只改了 `s1-1` 作样板**，其余 9 节待改 |
| 6 | 知识关联不该单列成模块 | ⚠️ **规则已落文**（§二：承上写进前置知识、启下写进问题陈述、横向写在概念旁）；模块已折叠降权，**但 10 节的内容还没融进正文** |
| 7 | 编排要主次清晰、循序渐进 | ✅ 阅读动线 + 三条硬要求已落文（§一）；三层分层已在 CSS 落地 |
| 8 | 必要处加函数图像（数形结合） | ⚠️ **`s1-1` 已加 2 幅手写 SVG**（映射示意 + 定义域数轴叠加）；**其余 9 节的图待补**，清单见 §四.1；`viz.js` 仍是线代专用，交互图需先扩引擎 |

---

## 5. 下一步任务清单（按优先级）

1. **`ch1.html` 10 节的 `.link-box` 融进正文后删除该模块**（§二给了三处投放位置）
2. **按 `编排与文风规范.md` §三 改写 10 节文字** ← 工作量最大的一项。`s1-1` 是样板
3. **补 9 张图**（`s1-2`…`s1-10`），清单与规格见 §四
4. **给 `viz.js` 加高数图形类型**，再把交互图接到对应节（当前它只有线代 4 个图形，**不能直接用**）
5. 缩短 `.app-box` / `.map-note` 正文（折叠后仍嫌长）
6. 写第 2 章 5 节、托马斯补充 2 节
7. `index.html` 收尾、建 `exam.html` / `cheatsheet.html`、把 `site.test.js` 对齐高数站口径
   （它现在仍是线代站的断言：6 小节 / 21 节 / 50 学时，**预期失败**，见 README §八）

---

## 6. 工程结构

```
高等数学学习站/
├── index.html                 首页（12 章课程地图）— 整页纯中文
├── chapters/ch1.html          第 1 章：10 节 + 章末测验（约 4300 行）
├── chapters/ch2.html          第 2 章：占位页
├── chapters/supp1.html        托马斯补充一：占位页
├── assets/css/
│   ├── theme-tokens.css       4 套主题 × 亮/暗色板（由 tools/gen-themes.py 生成，勿手改）
│   └── style.css              全站样式（§12.5 双语 / §12.5b 视频入口 / §13b 纯中文模式 /
│                              §13c 讲评内边距 / §13d 三层分层 / §13e 图解）
├── assets/js/
│   ├── quiz.js                测验引擎（判分 / 难度重排 / 讲评报告 / 订正）
│   ├── site.js                站点主控（目录 / 进度 / 学时徽章）
│   ├── videos.js              视频入口：集中配置（分 P + 时长）+ 渲染器
│   ├── theme.js               显示偏好：4 套主题 / 亮暗三态 / 中英语言开关
│   └── viz.js                 交互图形引擎（⚠️ 目前是线代专用，高数未启用）
├── tests/                     3 个 python 校验 + 6 个 js 测试（见 §2）
└── tools/
    ├── gen-themes.py          主题色板生成器（算色 + 断言）
    ├── gen-videos.py          从 B 站接口核对分 P 与时长
    └── section-format.md      单节施工格式契约（15 条铁律 + 可照抄模板）
```

---

## 7. 三条不可动摇的约定

1. **不许引入构建工具**：没有 npm 打包步骤，双击 HTML 就能开，唯一网络依赖是 MathJax CDN。
2. **不许复制教材正文**：托马斯 PDF 只用于核实结构与术语；宋浩视频只外链，不嵌入不转存。
3. **改完必须跑校验**：`check-structure` / `check-bilingual` / `check-html` 至少这三个；
   动了 CSS 或主题再加 `check-contrast` 与 `check-layout`（都是真实浏览器）。

---

## 8. v0.7.3 已做的文档口径修正（2026-09-16，接手前必读）

本轮**只改文档、术语与校验脚本，正文 `chapters/*.html` 一行未动**。详见 `CHANGELOG.md` v0.7.3。

**开工前请以这些文件为准**（它们的旧口径已作废）：

| 事项 | 旧口径（已作废，别再照抄） | 现口径 |
|---|---|---|
| 视频入口 | 讲解区**顶部 + 底部各一个** | **只有一个，在顶部**（`data-video-pos="top"`）；写底部会被 `check-bilingual.py` 报错 |
| 知识关联 | 每节必备 `.link-box` 模块 | **已取消**：承上→「前置知识」、启下→「本节要解决的问题」末句、横向→概念旁；判据 ≥3 处带 §号回指 |
| 测验题数 | 固定 7 题 | **7–9 题**，配比 ≈30/40/30 |
| 双语形式 | v0.3「英文在前」 | **v0.4「中文在前」**，全站唯一口径 |
| 易错点/考察要点 | 要写英文（两组 `<ul>`） | **只写中文**，不放 `.bi`；`check-structure.py` 会正向检查不含 `.en` |
| 校验器 `check-structure.py` | 退出码 1，报 122 个"可疑标签" | **假报已修**（SVG 白名单），现在只报 7 个待建小节 |
| 教材题目版权 | 不复制任何教材例题/习题 | **可用教材典型题目，但必须逐题核实答案**（见 `tools/section-format.md` §3） |
| 术语「奇偶性」 | `parity` | **改用 `even / odd`**（`parity` 经核实不是函数语境的标准词，见 `术语表.md` §三·补） |

### 仍未裁决（正文未动）

1. **`s1-8` / `s1-9` 的定理编号错位 + 全章重号**（`定义 1.5` 出现 4 次等）——
   甲/乙两套重排方案写在 `对照偏差清单.md` **D-11-a**，等用户选定。
   该条目还列出执行前必须知道的两件事：**`性质` 必须与 `定理` 一起处理**，
   以及**正文 169 处编号引用里 10 处是跨节裸引用，必须补节号**
2. `ch1.html` 第 274、280 行的 `parity` 待改（随第 1 项一并做）
3. 正文中文侧是否给「陪域」补英文标注（偏差清单 D-02）

### 九套校验的实测基线（2026-09-16，本机复跑）

```bash
python3 tests/check-structure.py   # exit 1 —— 仅 7 个待建小节；已建内容全绿（原 122 条 SVG 假报已修）
python3 tests/check-bilingual.py   # exit 0 —— 硬问题 0
python3 tests/check-html.py        # exit 1 —— 仅 exam.html / cheatsheet.html 未建
cd tests
node quiz.test.js       # 通过 36 项
node review.test.js     # 通过 77 项
node videos.test.js     # 通过 37 项
node theme.test.js      # 通过 62 项（含 85 个双语容器"中文在前"断言）
node check-layout.js    # 三档无溢出；7253 个行内公式基线差 0.0px；exit 1 仅因 2 个未建页
node check-contrast.js  # exit 0；8 个"主题×模式"组合无 <4.5:1
```

**环境实测**：node v24.21.0 · npm 11.19.0 · Python 3.13.15 · Microsoft Edge 已装。
`cd tests && npm install` 实测 9 秒装完 33 包，**无需 `--cache` 参数**
（`puppeteer-core` 是纯 JS 包，不下载浏览器）。
MathJax 本地副本放在 `/tmp/mj/tex-svg.js` 供两个浏览器校验做请求拦截，保证测量可复现。

### 工程完成计划

见同目录 **`工程完成计划（待确认）.md`**（A 类第 1 章改造 5 项 / B 类剩余 7 节 / C 类收尾 4 项，
含建议的 4 轮施工顺序与每轮验收命令）。

---

## 9. 已知的"预期失败"与遗留项（v0.7.2 原始记录，部分已被 §8 修复）

| 项 | 说明 |
|---|---|
| `site.test.js` 失败 | 仍是线代站口径（6 小节 / 21 节 / 50 学时 / `exam.html` 28 题）。**必须等第 1 期 17 节建完才能定稿** |
| `check-structure` / `check-html` 退出码为 1 | 因为报的是**未建项**（`ch2` / `supp1` 的 7 节、`exam.html`、`cheatsheet.html`），不是缺陷。`check-structure` 原有的 122 条 SVG 假报**已于 v0.7.3 修掉** |
| `viz.js` 未接入 | 见 §5 第 4 项 |
| `.model-box` 无样式 | 线代站要求每节一个模型题块；`style.css` 里没有这四个类，故高数站第 1 期不用 |
| `s1-1` 是唯一"文风样板"节 | 其余 9 节仍是旧文风（AI 味、知识关联单列、缺图） |


| 项 | 说明 |
|---|---|
| `site.test.js` 失败 | 仍是线代站口径（6 小节 / 21 节 / 50 学时 / `exam.html` 28 题）。**必须等第 1 期 17 节建完才能定稿** |
| `check-structure` / `check-html` 退出码为 1 | 因为报的是**未建项**（`ch2` / `supp1` 的 7 节、`exam.html`、`cheatsheet.html`），不是缺陷 |
| `viz.js` 未接入 | 见 §5 第 4 项 |
| `.model-box` 无样式 | 线代站要求每节一个模型题块；`style.css` 里没有这四个类，故高数站第 1 期不用 |
| `s1-1` 是唯一"文风样板"节 | 其余 9 节仍是旧文风（AI 味、知识关联单列、缺图） |
