# 章节页撰写规范（高等数学学习站）

> 本文件规定 `chapters/*.html` 的统一结构、可用 CSS 类、双语口径与内容深度要求。
> **所有章节页必须严格照此执行**，以保证全站风格一致、脚本能正确工作。
>
> 本文件基于**线性代数学习站**的 `写作规范.md` 改写，并落实两项新要求：
> ① **中英等权**（§三·补六，2026-09-15 用户补充）；② 视频跳转与托马斯映射（§三·补八、§三·补九）。
>
> **本文件是活文档。** 凡与 `01_交接文档/` 里的旧描述冲突，以本文件为准——本文件是随施工同步更新的那一份。

---

## 一、页面骨架（直接复制，只改标注处）

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>第N章 章节名 · 高等数学学习站</title>
<meta name="description" content="一句话描述本章内容。建议中英各一句。">
<link rel="stylesheet" href="../assets/css/style.css">
<script>
  window.MathJax = {
    tex: { inlineMath: [['$', '$'], ['\\(', '\\)']], displayMath: [['$$', '$$'], ['\\[', '\\]']] },
    options: { skipHtmlTags: ['script', 'noscript', 'style', 'textarea', 'pre', 'code'] },
    svg: { fontCache: 'global' }
  };
</script>
<script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js"></script>
</head>
<body data-page="chN" data-chapter="N">

<header class="site-header">
  <a class="brand" href="../index.html">
    <span class="brand-mark">AM</span>
    <span>高等数学学习站<small>同济八版骨架 · 托马斯映射 · 中英对照</small></span>
  </a>
  <button class="nav-toggle" type="button" aria-label="切换菜单">☰</button>
  <nav class="header-nav">
    <a href="../index.html">首页</a>
    <a href="ch1.html">第1章 函数与极限</a>
    <a href="ch2.html">第2章 导数与微分</a>
    <a href="supp1.html">托马斯补充</a>
    <!-- 未建成的章节一律用 <span class="nav-soon">，不要写 href，
         否则 check-html.py 会报失效链接 -->
  </nav>
  <div class="global-progress"></div>
</header>

<div class="layout">
  <aside class="sidebar">
    <div class="toc-title">课程导航</div>
    <nav class="toc">
      <div class="toc-group">
        <a class="toc-chapter" href="ch1.html"><span class="num">1</span>函数与极限</a>
        <a class="toc-chapter" href="ch2.html"><span class="num">2</span>导数与微分</a>
        <span class="toc-chapter is-soon"><span class="num">3</span>微分中值定理与导数应用</span>
        <!-- …… 4–12 章同样处理 …… -->
        <a class="toc-chapter" href="supp1.html"><span class="num">TX</span>托马斯补充</a>
      </div>
      <div class="toc-group">
        <div class="toc-title">工具</div>
        <span class="toc-chapter is-soon">公式速查表</span>
        <span class="toc-chapter is-soon">综合自测卷</span>
      </div>
    </nav>
    <div class="toc-title">本页目录</div>
    <nav class="toc" data-toc></nav>
  </aside>

  <main class="main">
    <div class="content">

      <div class="page-head">
        <div class="page-eyebrow">CHAPTER N · 同济八版第 N 章 / Thomas 14e Ch.X</div>
        <h1>第 N 章　章节名</h1>
        <p class="lede">中文：2–3 句话说清本章解决什么问题、在整门课中的位置。</p>
        <p class="en" lang="en">English: the same, in two or three sentences.</p>
      </div>

      <!-- ======== 以下为小节内容 ======== -->
      <h2 id="s1-1" data-hours="2" ...>1-1 标题 / English Title</h2>
      ...
      <!-- ======== 小节内容结束 ======== -->

      <h2 id="final">第 N 章 · 综合测验</h2>
      <p>...</p>
      <div class="quiz" data-quiz-id="chN-final" data-title="第 N 章综合测验" data-pass="0.7">
        ...
      </div>

      <nav class="chapter-nav">
        <a class="prev" href="chN-1.html"><span class="dir">← 上一章</span><span class="ttl">第 N-1 章 ...</span></a>
        <a class="next" href="chN+1.html"><span class="dir">下一章 →</span><span class="ttl">第 N+1 章 ...</span></a>
      </nav>

    </div>
  </main>
</div>

<footer class="site-footer">
  <p><strong>高等数学学习站</strong> · 同济八版骨架 + 托马斯 14 版映射 · 中英对照</p>
  <p>进度与成绩保存在本机浏览器，不上传任何数据。</p>
</footer>

<script src="../assets/js/viz.js"></script>
<script src="../assets/js/quiz.js"></script>
<script src="../assets/js/site.js"></script>
</body>
</html>
```

**注意**：

- `<body>` 的 `data-page` 必须是**不带扩展名的文件名**（`ch1`、`ch2`、`supp1`），`data-chapter` 是章号（托马斯补充章用 `101`）。
- `h2` 的 `id` 必须与 `assets/js/site.js` 的 `COURSE` 数组**逐字一致**：小节是 `s<章号>-<节号>`（`s1-1`），补充章是 `tx<补充号>-<节号>`（`tx1-1`），章末综合测验是 `final`。
- `h2` 标题文字里带编号（`1-1 …`）是正常的。
- **未建成的页面/章节不要写成 `<a href>`**，用 `<span class="is-soon">`（侧栏）或 `<span class="nav-soon">`（顶栏）。建成的当天再换成真实链接。

---

## 二、可用内容块（只能用这些类名）

### 1. 定义
```html
<div class="box def-box">
  <div class="box-title"><span class="ico">📘</span>定义 1.1　映射 / Mapping</div>
  <p class="zh">设 $X$、$Y$ 是两个非空集合……</p>
  <p class="en" lang="en">Let $X$ and $Y$ be two nonempty sets……</p>
</div>
```
> 定义要写得**可直接默写**。

### 2. 定理 / 判定 / 性质
```html
<div class="box thm-box">
  <div class="box-title"><span class="ico">📐</span>判定 1.1　标题 / English Title</div>
  <p class="zh"><span class="thm-name">判定 1.1</span>　……</p>
  <p class="en" lang="en"><span class="thm-name">Criterion 1.1</span>　……</p>
</div>
```

### 3. 计算步骤 / 方法（会自动编号）
```html
<div class="box method-box">
  <div class="box-title"><span class="ico">⚙️</span>方法 1.1　求函数的定义域 / Finding the Domain</div>
  <p class="zh">……</p>
  <ol>
    <li class="zh">分式：分母 $\ne 0$。</li>
    <li class="zh">偶次根式：被开方式 $\ge 0$。</li>
  </ol>
  <p class="en" lang="en">……</p>
  <ol>
    <li class="en" lang="en">Fraction: the denominator must be nonzero.</li>
    <li class="en" lang="en">Even root: the radicand must be $\ge 0$.</li>
  </ol>
</div>
```

### 4. 易错 / 注意
```html
<div class="box warn-box">
  <div class="box-title"><span class="ico">⚠️</span>易错点 / Common Pitfalls</div>
  <ul>
    <li class="zh">……</li>
  </ul>
  <ul>
    <li class="en" lang="en">……</li>
  </ul>
</div>
```

### 5. 考察要点
```html
<div class="box exam-box">
  <div class="box-title"><span class="ico">🎯</span>考察要点 / How This Is Tested</div>
  <ul>
    <li class="zh"><span class="tag">高频</span>……</li>
  </ul>
  <ul>
    <li class="en" lang="en"><span class="tag">Frequent</span> ……</li>
  </ul>
</div>
```
> `.tag` 里的档位词可中英各一套：`高频`/`Frequent`、`小题`/`Short`、`易错`/`Trap`、`考研`/`Exam`。

### 6. 直观理解 / 为什么
```html
<div class="box insight-box">
  <div class="box-title"><span class="ico">💡</span>中文标题 / English Title</div>
  <p class="zh">……</p>
  <p class="en" lang="en">……</p>
</div>
```

### 7. 工程应用（**不加英文**）
```html
<div class="box app-box">
  <div class="box-title"><span class="ico">🔧</span>工程联系</div>
  <p>转子动力学中，叶片盘的振动模态求解最终归结为广义特征值问题 $Kx=\lambda Mx$……</p>
</div>
```
> 见 §三·补六 的豁免清单：**概念扩展应用**不加英文。

### 8. 例题（**不加英文**，必须给完整解答）
```html
<div class="example">
  <div class="example-head">
    <span class="badge">例 1.3</span> 简短说明
    <span class="lv">中等</span>
  </div>
  <div class="example-body">
    <p>题目……</p>
    <div class="solution">
      <p class="solution-label">解</p>
      <p>第一步……</p>
    </div>
  </div>
</div>
```
> `.lv` 里填 `基础` / `中等` / `考研` / `易错`。**解答的每一步都要写出理由**，不跳步。
> 例题属于「例题讲解」，不加英文对照；但作者鼓励在**验算环节**保留「回代检验」这类自查步骤。

### 9. 折叠答案（自己先做）
```html
<details class="reveal">
  <summary>点击查看解答</summary>
  <div class="reveal-body">……</div>
</details>
```

### 10. 表格
```html
<div class="tbl-wrap">
  <table>
    <thead><tr><th>情形</th><th>结论</th></tr></thead>
    <tbody><tr><td>……</td><td>……</td></tr></tbody>
  </table>
</div>
```

---

## 三、测验写法（脚本会自动变成交互题）

```html
<div class="quiz" data-quiz-id="ch1-1" data-title="1-1 标题 · 自测 / Title · Self-Test" data-pass="0.7">

  <!-- 单选题 -->
  <div class="q" data-type="single" data-level="基础">
    <div class="q-stem">
      <p>中文题干……</p>
      <p class="q-stem-en">English stem……</p>
    </div>
    <ul class="options">
      <li data-correct="true"><span class="opt-body">$-2$</span></li>
      <li><span class="opt-body">$-1$</span></li>
    </ul>
    <div class="q-explain">
      <p>中文解析：点明每个干扰项错在哪。</p>
      <p class="en" lang="en">English summary of the correct choice and the traps.</p>
    </div>
  </div>

  <!-- 多选题：多个 li 加 data-correct="true" -->
  <div class="q" data-type="multi" data-level="中等"> … </div>

  <!-- 判断题：两个 li，其中一个 data-correct="true" -->
  <div class="q" data-type="judge" data-level="中等"> … </div>

  <!-- 填空题：data-answer 用 | 分隔所有可接受写法 -->
  <div class="q" data-type="fill" data-level="考研">
    <div class="q-stem">
      <p>中文题干……</p>
      <p class="q-stem-en">English stem……</p>
    </div>
    <div class="q-answer" data-answer="16|16."></div>
    <div class="q-explain">
      <p>中文解析……</p>
      <p class="en" lang="en">English summary.</p>
    </div>
  </div>

  <div class="q-review">
    <p>本卷讲评：……</p>
  </div>

</div>
```

### 🔴 三条不能破的结构约束

1. **`.q-stem` 必须是 `<div>`，英文题干 `.q-stem-en` 必须是它的子元素。**
   `quiz.js` 会把 `.q-stem` 移进 `.q-head`（`display:flex`）。若英文题干是兄弟节点，会被挤进同一行；若 `.q-stem` 用 `<p>`，浏览器会因"`<p>` 不能嵌套 `<p>`"而把结构拆坏。
   `tests/check-structure.py` 会检查这两点。

2. **`data-level` 只有三个合法值**：`基础` / `中等` / `考研`。排序不用管（`quiz.js` 会按难度稳定重排），但**标错档位比排错顺序严重得多**。

3. **每题必须有 `.q-explain`，且要点明每个错误干扰项错在哪。**
   这是本站与普通题库最大的差别：错题要能教会人。不要放一眼排除的荒谬选项。

### 测验硬性要求

- 每小节 1 个测验，`data-quiz-id` 命名 `ch<章号>-<小节号>`（如 `ch3-2`）；
  托马斯补充章用 `tx<补充号>-<节号>`；每章末尾 1 个综合测验，`data-quiz-id="chN-final"`。
  **全站唯一**，由 `check-structure.py` 检查。
- 每份测验 **7–9 题**（第 1 期起从 6–9 调高），题号由脚本自动生成，**不要自己写题号**。
- 题型搭配：单选约 50%、多选约 20%、判断约 15%、填空约 15%。
- `data-answer` 必须穷举合理写法（如 `-2|−2`），脚本已自动忽略空格并做全角转半角。
- 每份测验**必须**有 `.q-review` 讲评（见 §三·补四）。

---

## 三·补　难度基准：**循序渐进，上限到考研数学一**

> ⚠️ 硬要求。**整门课是一条平滑上升的坡道，不是一段段高台。**

### 各处的难度配比

| 位置 | 基础 | 中等 | 考研 | 说明 |
|---|---|---|---|---|
| 每节自测（7–9 题） | ~30% | ~40% | ~30% | 考研题放最后 2–3 道 |
| 章末综合测验 | ~25% | ~35% | ~40% | 必须有跨小节综合题 |
| 综合自测卷（`exam.html`） | ~25% | ~35% | ~40% | 全站难度峰值 |

### 跨章节也要递增（**注意：与线代站不同，本课程考研题占比更高**）

| 章节 | 考研题占比 | 理由 |
|---|---|---|
| 第 1–2 章 | ~20% | 打地基，重在把定义与计算做准 |
| 第 3–6 章 | ~25–30% | 计算量上来，开始出现含参数讨论 |
| 第 7 章 | ~30% | 微分方程是考研大题必考 |
| 第 8–10 章 | ~35% | 多元函数与重积分，几何直观要求高 |
| 第 11–12 章 | ~40% | 曲线曲面积分、无穷级数，考研压轴常客 |

### 例题也要有梯度

`.example` 的 `.lv` 用 `基础` / `中等` / `考研` / `易错` 四档，顺序 **基础 → 中等 → 考研**，且**每节至少 1 道 `考研` 级例题**。

---

## 三·补四　讲评环节：每份测验都要写 `.q-review`

学生提交测验后，脚本会自动生成讲评报告的五部分中的四部分，**作者只需写 ④ 讲评**：

```html
<div class="q-review">
  <p>本卷讲评：这一卷围绕一条主线——……</p>
  <p>最常见的错法有……</p>
  <p>与后文的接口有两处要提前记住……</p>
  <p>下一步建议：……</p>
</div>
```

**讲评 ≠ 解析。** 讲评回答**整卷层面**的四件事：

1. **本卷在考什么** —— 一句话点出考查主线，以及它在整章里的位置
2. **最常见的错法** —— 从干扰项设计看，学生最容易栽在哪、为什么
3. **知识之间的联系** —— 向前接哪一节、向后铺到哪一章
4. **下一步建议** —— 做完这份卷子该干什么（继续往下 / 回读某节 / 二轮再攻）

篇幅 **250–500 字**，用 `<p>` 分段，可用 `<strong>` 强调。
**讲评属于"教师视角解说"，不加英文。**

---

## 三·补五　交互式可视化（`.viz`）

页面需引入 `viz.js`（放在 `quiz.js` 之前）。作者只写一个空容器：

```html
<div class="viz" data-viz="det2-area" data-a="3,1" data-b="1,2"></div>
```

| `data-viz` | 用途 | 参数 |
|---|---|---|
| `det2-area` | 二阶行列式 = 平行四边形面积 | `data-a="x,y"` `data-b="x,y"` |
| `det-shear` | 倍加性质：剪切不改变面积 | `data-a` `data-b` |
| `eigen-explore` | 拖向量找特征方向 | `data-matrix="a,b;c,d"` `data-vec="x,y"` |
| `eigen-ellipse` | 单位圆 → 椭圆 | `data-matrix="a,b;c,d"` |

> ⚠️ **本仓库当前的 `viz.js` 是从线代站原样复制过来的，四个图形全部是线代专用**
> （行列式面积、剪切、特征向量、单位圆→椭圆）。高等数学要用可视化时，
> **必须先扩充 `viz.js` 的图形类型**，不要直接把这些 `data-viz` 值抄进高数章节页——
> 那会在页面上渲染出与本节无关的线代图形。
> 本文件在 `viz.js` 扩充完成前，**不建议**在高数章节页使用 `.viz`。

**放置位置**：紧跟对应的 `.insight-box` 之后。图形下方不要重复文字描述（`.viz-caption` 由脚本生成）。

---

## 三·补六　中英双语：**等权**（本规范最重要的一节）

> **2026-09-15 用户补充要求（原话）：**
> "中英文在这其中要有相同的比例，除了概念扩展应用、错/例题讲解和使用说明不用体现，
> 在 UI 和其他部分中英的权重应当相同。"

这一条**覆盖**交接文档 §4.1／§4.2 的旧口径。旧口径只要求"定义/定理/方法/题干"双语，
其余留白；新口径要求**凡是"讲解与规范"性质的内容，中英并列且权重相同**。

### 6.1 覆盖清单（必须做到）

| 位置 | 双语写法 |
|---|---|
| 页面 `<title>` | 中文即可（浏览器标签宽度有限），但 `<meta name="description">` **中英各一句** |
| `.page-head` 的 `<h1>` | 中文；`page-eyebrow` 已含英文教材章号 |
| `.page-head` 的 `.lede` | 中文段 + `.en` 段 |
| 小节 `h2` | **`1-1 映射与函数 / Mappings and Functions`**（中文 + ` / ` + 英文标题） |
| 「前置知识」「本节要解决的问题」段 | 中文段 + `.en` 段（英文段以 `<strong>Prerequisites.</strong>` / `<strong>The problem this section solves.</strong>` 起头） |
| `.insight-box` | `.zh` 段组 + `.en` 段组（标题写成 `中文标题 / English Title`） |
| `.def-box` / `.thm-box` / `.method-box` | `.zh` 与 `.en` **成对**；标题写成 `定义 1.1　映射 / Mapping` |
| `.warn-box` | 两组 `<ul>`：`<li class="zh">` 一组、`<li class="en" lang="en">` 一组 |
| `.exam-box` | 同上，两组 `<ul>` |
| `.link-box` | 同上，两组 `<ul>`（`承上` / `From before`、`启下` / `Onward`、`横向` / `Sideways`） |
| `.map-note` | `.zh` 段组 + `.en` 段组 |
| 测验 `data-title` | `1-1 映射与函数 · 自测 / Mappings and Functions · Self-Test` |
| 测验题干 | 中文 `<p>` + `.q-stem-en` |
| 测验解析 `.q-explain` | 中文 `<p>` + `.en` 段（**英文一句要点即可，不求详尽**） |
| 视频入口（`videos.js` 渲染） | 顶部/底部两个标题：「先看视频讲解」/「看完还想听一遍？」按要求为**使用说明**，只中文；按钮本体由脚本生成，中英混排会挤爆窄屏 |

### 6.2 豁免清单（**不加英文**）

| 位置 | 理由 |
|---|---|
| `.app-box` 工程联系 | 概念**扩展应用** |
| `.example` 及 `.solution` 全部内容 | **例题讲解** |
| `.q-review` 讲评 | 属**例题/错题讲解**的延伸 |
| **`.warn-box` 易错点** | **2026-09-16 用户要求：这部分只要中文** |
| **`.exam-box` 考察要点** | 同上 |
| **`.link-box` 知识关联** | 同上 |
| **`.map-note` 教材映射** | 同上 |
| **首页 `index.html` 整页** | **2026-09-16 用户要求：「index 这个部分都不要英文」** |
| （`index.html` 里保留的英文只有字面量：`localStorage`、`target="_blank"`、`rel="noopener noreferrer"` 等代码标识符，以及 `<meta>` 里的英文摘要——前者不是"说明文字"，后者不在页面上显示） | |
| `.video-link` 视频跳转 | **使用说明** |
| `<details class="reveal">` 折叠答案 | 属解答 |
| 站内导航（顶栏、侧栏、`.chapter-nav`）与 `check-layout.js` 等工具输出 | **UI 骨架**，中英混排会挤爆导航条；正文的 UI 文案（如标题栏、按钮由脚本生成）已由脚本统一 |

> 若你希望**导航也中英并列**，请明说——代价是顶栏在 1280px 下会换行、侧栏每项占两行。
> 默认不做。

### 6.3 写法要求：**整段对照**（中文段在前、英文段在后）

> **形式沿革（别再改回去，两次都是按用户反馈定的）：**
>
> | 版本 | 形式 | 结果 |
> |---|---|---|
> | v0.1 | 整段 `.zh` + 整段 `.en`（中文在前） | 可用 |
> | v0.2 | 逐行 `.pair`（英文行/中文行逐句交替） | ❌ **废弃** —— 用户反馈"太混乱了不好阅读" |
> | v0.3 | 整段 `.en` + 整段 `.zh`（英文段在前） | ❌ **废弃** —— 与 v0.1 的顺序相反，两版并存导致施工方各写一半 |
> | **v0.4** | **整段 `.zh` + 整段 `.en`（中文段在前，英文段在后）** | ← 当前，**全站唯一口径** |> 
>
> 注意 v0.1→v0.3 不只换了顺序，还保留了 v0.2 期定下的**中英等权**：
> 英文段不再降字号、不降颜色，只靠**衬线体**与**左侧色条**区分。

```html
<div class="bi">                                   <!-- 双语容器 -->
  <!-- 先：中文段组 -->
  <p class="zh" lang="zh">中文第 1 段。</p>
  <p class="zh" lang="zh">中文第 2 段。</p>
  <!-- 后：英文段组（段数与中文一一对应） -->
  <p class="en" lang="en">English paragraph 1.</p>
  <p class="en" lang="en">English paragraph 2.</p>
</div>
```

**硬性规则**：

1. **中文段在前、英文段在后**，同一容器内先排完所有 `.zh` 再排所有 `.en`。
   > ⚠️ **全站只有这一种顺序，不得因段落不同而颠倒。**
   > **检索范围必须包含 `index.html`**：它一度整页没有 `.bi` 容器、
   > hero 又是英文在前，结果三重漏检（翻转脚本、`check-bilingual.py`、`theme.test.js` 都没覆盖它）。
   > 现在两处检查都已纳入 `index.html`。 这一条踩过坑：
   > v0.3 的规范写"英文在前"、而施工契约与已建页面是"中文在前"，
   > 结果 9 个并行作业里有一部分按规范写、一部分按契约写，组装后同一页出现两种顺序。
   > 改完必须做**全量检索**确认无残留（`tests/theme.test.js` 的【5】就是干这个的，
   > 它会遍历所有 `.bi` 容器断言"首个段必须是 `.zh`"）。
2. **段数必须一一对应**：英文 3 段，中文就 3 段。
   > ⚠️ **豁免清单里的块（见 §6.2）不属于 `.bi`**：它们只有中文段、没有 `.bi` 容器。
   > `check-bilingual.py` 对这两类块分别把关——
   > 该双语的查"段数相等"，该纯中文的查"不得出现 `.en` 或 `.bi`"。
   `tests/check-bilingual.py` 会检查每个 `.bi` 容器里 `.en` 与 `.zh` 的**段数是否相等**，
   以及**有无单侧语言**（只有英文段或只有中文段）。
3. **公式、数字、节号（`§1.1`、`1.2–1.7`）、人名、URL 两侧原样保留、位置对应，不译。**
   `check-bilingual.py` 会校验：
   - **独立公式段（`$$…$$`）两侧必须逐字一致** —— 这是硬错误。
     注意 `\frac` 与 `\dfrac` 渲染行高不同，**算不一致**，必须统一。
   - 行内公式要求"互含"（一方的公式要能在另一方公式串里找到），不满足的登记为待确认。
4. **发现漏译、增译、数值或公式不一致时，不擅自改动**，记入 `对照偏差清单.md` 交回确认。
5. **术语**：中文术语首次出现写「中文（English）」，此后只用中文 —— 见 §6.3b。
   这条与整段对照**不冲突**：段里该出现的英文照样出现，只是中文段不再重复加英文括号。

### 6.3a 已废弃的逐行对照写法（`.pair`）

v0.2 期用过、**现已不用**：

```html
<!-- ❌ 已废弃：太碎，读不下去 -->
<div class="bi">
  <div class="pair">
    <p class="pair-en" lang="en">English sentence.</p>
    <p class="pair-zh" lang="zh">中文句子。</p>
  </div>
  <!-- …每个句子一对… -->
</div>
```

CSS 里 `.pair` 那套**保留着**（§12.5a），万一哪天又有内容确实需要逐句对照可以直接用。
正文里已经**没有任何** `.pair`，`[keep-together]` 标记也随 v0.3 一并去掉了
（整段对照不需要处理"切点错位"）。

### 6.3b 术语：首次出现标注，此后只用中文

**中文术语首次出现时写成「中文（English）」，同页此后只用中文。**

```html
<!-- 首次出现：中英并列 -->
<span class="term">映射</span>（<span class="term-en">mapping</span>）

<!-- 此后同一术语不再加英文 -->
…这个<strong>映射</strong>是单值的…
```

- 术语表在 `术语表.md`，**新增术语必须先登记再使用**，
  否则英文侧会各页叫法不一（一处 `parity`、一处 `evenness`）。
- 英文术语以 **Thomas Calculus 14th ed.** 原文为权威；中文以**同济八版**为准。
  两者冲突时中文侧用同济、英文侧用托马斯，并写进术语表的「差异备注」。
- 注意这条与"逐行对照"**不冲突**：逐行对照针对**句子**，
  术语首现标注针对**词**——英文行里该出现的术语照样出现，只是中文行不再重复加英文括号。

### 6.3b-2 英文字体：独立指定，不跟随中文回退

```css
--font:    /* 中文字体栈：中文行专用 */
           -apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB",
           "Microsoft YaHei", "Source Han Sans SC", "Noto Sans CJK SC", "Segoe UI", sans-serif;
--font-en: /* 英文字体栈：**必须有完整 fallback，末尾回退系统默认** */
           "Source Serif 4", "Source Serif Pro", Charter, "Bitstream Charter",
           "Iowan Old Style", Georgia, Cambria, "Times New Roman",
           "PingFang SC", "Microsoft YaHei", serif;
```

**为什么选衬线系（Source Serif 4 / Charter / Georgia）而不是 Inter / System UI 无衬线系**：

1. **与数学字体同族。** 本站满页行内公式；英文若用无衬线，公式（MathJax 的 SVG 字形是衬线）
   与正文会像两种材料拼在一起。衬线体让 `the limit of $f(x)$ as $x\to0$` 看起来是一句话。
2. **这是精读材料。** 定义要能默写、例题要逐步看，Source Serif 4 / Charter 的 **x-height 偏大**、
   字重区分清楚、**Italic 与数字清晰**，比无衬线更适合长段落连续阅读。
3. **系统自带率高，不会跳到难读的默认值。** Charter 在 macOS 内置，Georgia 在 Windows/macOS 都有；
   取不到时先回退中文栈（保证不出现豆腐块），最后回退 `serif`。

**行高与字号**：英文行 `line-height: 1.86`，中文行 `1.78` ——
**英文行高略大于中文行**，补偿拉丁字母的 x-height 差，两行视觉重量才相同。
字号两者都是 `1rem`：Source Serif 4 / Charter 的 x-height 偏大，同字号下不会显小；
**若改用 Inter 系，需下调到 `.96rem` 才平衡**。

**断词**：`.en` 必须带 `hyphens: auto` 与 `overflow-wrap: break-word`。
英文长词（`aerodynamic`、`differentiable`）在 320px 下会顶宽页面，这是实测过的。

**公式不套正文字体**：不要给 `mjx-container` 或其任何后代设 `font-family`，
公式字形由 MathJax 的 SVG 决定。

### 6.3c 旧写法（`.zh` / `.en`，保留兼容，不再使用）

```html
<p class="zh">中文表述……</p>
<p class="en" lang="en">English statement……</p>
```

`.en` 类仍被**测验解析**（`.q-explain` 里的英文要点）与 `.q-stem-en` 使用，故 CSS 保留。
2. **`lang="en"` 属性不能省**（影响断词与屏幕阅读器）。
3. **英文是"可独立阅读的完整版"，不是关键词堆砌。** 定义要完整译出，包括条件与限定语。
   但**测验解析的英文可以只是一句要点**——这是唯一允许"英文短于中文"的位置。
4. **数学符号与公式原样保留**，只翻译自然语言。公式里的变量名不要改。
5. **术语首次出现时中英并列**：`<span class="term">映射</span>（<span class="term-en">mapping</span>）`。
6. **中文正文一个字都不改**——加英文是"追加"，不是"改写"。
7. **英文段落不要用直角引号**，用 `"` `'` 或 LaTeX 的 `\text{...}`。
8. **不要给英文段落加 `text-align: justify`** ——会在窄屏产生难看的字间距。

### 6.3d 中英语言开关：每处英文都必须"可隐藏"

顶栏有一个**语言开关**（`中英` ⇄ `中`），纯中文模式下要把英文整段隐藏。
因此**新写的英文必须放进下面三种载体之一**，否则纯中文模式会漏出英文：

| 载体 | 用途 |
|---|---|
| `<p class="en">`（在 `.bi` 容器内） | 成段的英文 |
| `<p class="q-stem-en">` | 测验英文题干 |
| `<span class="en-inline">` | **行内**英文：h2 标题、块标题、`page-eyebrow` 里的 ` / English` |

**允许保留、不需要隐藏**的拉丁文本（已写进校验白名单）：

- **术语首现标注**：`（<span class="term-en">mapping</span>）` —— 它是中文句子的一部分，
  纯中文模式下保留（否则"术语首现中英并列"这条规定就失效了）
- **代码/标识符字面量**：`localStorage`、`target="_blank"`、`rel="noopener noreferrer"`
- **单位与人名/书名**：`rad`、`Lanczos`、`Thomas`、`Calculus`
- **`.map-note` 整块**：该块"只要中文"，但它的用途就是给出英文教材的节名
  （`§7.1 Inverse Functions`），隐藏掉这块就没用了

**自动护栏**：`check-bilingual.py` 的 `check_zh_mode_leak()` 会**模拟纯中文模式** ——
把上述三种载体与 `.term-en`、数学公式、`.map-note` 全部剥离，
再扫描剩下的可见文本；只要还有拉丁词就报错。
**这是这套双语机制的总护栏**，新写的英文漏在载体外一定会被它抓到。

### 6.4 权重相同怎么落实（不能只靠"都写了"）

"权重相同"包含**视觉权重**，否则英文虽然写了、读起来仍像附注。本仓库已在 `assets/css/style.css`
的 §12.5 落实：

- `.en` 与 `.q-stem-en` **不再缩小字号、不再降低颜色**（旧实现是 `.93em` + `--ink-soft`，英文明显轻一档）。
  现在中英只在**字体族**（英文用衬线体）与**左侧色条**上区分。
- `.term-en` 与 `.term` 对齐（同色、同粗、同虚线）。
- 新增的两条**空白与结构**要求：
  - 中英两段的段间距一致（`.zh` 与 `.en` 都是 `margin: .5rem 0`）；
  - 英文段必须有 `padding-left` 与 `border-left`，使其在版面上有明确边界，不被读成上一段的续行。

> **改动这一段 CSS 之后必须重跑** `node tests/check-layout.js`：
> 它守的是三档宽度无横向溢出 + 公式基线，而英文句子比中文长得多，是最容易顶破 320px 的内容。
> 实测：`ch1.html` 在 320px 下 667 个行内公式、容器与公式高度差 0.0px，无溢出。

---

## 三·补七　典型模型题

> 线代站要求每节一个 `.model-box`。**高数站第 1 期暂不启用** `.model-box`——
> `style.css` 里当前**没有** `.model-box` / `.model-item` / `.model-signal` / `.model-route` 的样式，
> 而 `check-structure.py` 也**不检查**它。若后续要加，必须先在 `style.css` 补齐这四个类，
> 并在校验脚本里加检查；否则写了也没有任何视觉效果。

第 1 期的替代做法：把"套路型题目"直接并入 `.example`（用 `.lv` 标 `中等`/`考研`），
并在 `.exam-box` 里写清识别信号（"看到 $X$ 就用 $Y$"）。

---

## 三·补八　视频跳转（`.video-link`）

**宋浩老师的视频只外链，不嵌入、不转存。**

### 视频信息只在 `assets/js/videos.js` 里，正文不硬编码 URL

> **2026-09-15 第二次改版**：原来把 `data-video="BV…"` 挂在 `h2` 上，
> 违反"集中配置、不把 URL 硬编码进正文"的要求。现改为 **`h2` 只放一个引用键**，
> 真正的标题 / URL / 平台 / 对应章节 / 分 P 全在 `videos.js`。

`h2` 只登记三件事：

```html
<h2 id="s1-1" data-hours="2"
    data-video-map="s1-1"
    data-video-title="《高等数学》同济版 全程教学视频（宋浩老师）">1-1 映射与函数 / Mappings and Functions</h2>
```

| 属性 | 必填 | 说明 |
|---|---|---|
| `data-video-map` | ✅ | 引用键，**必须等于小节 id**。`check-structure.py` 会校验一致 |
| `data-video-title` | ✅ | 合集全名（给人看，也是无 JS 时的回退信息） |

### 讲解区的入口（**只有一个，在顶部**）

```html
<!-- 紧跟 h2 之后：顶部入口，也是全节唯一入口 -->
<div data-video-entry="s1-1" data-video-pos="top"></div>

…正文…
```

> ⚠️ **2026-09-16 用户要求删除讲解区底部的"看完还想听一遍？"卡片。**
> 现在规定：**每个小节只有一个视频入口，位于讲解区顶部**（`data-video-pos="top"`）。
> **不要再写 `data-video-pos="bottom"`** —— `tests/check-bilingual.py` 会正向断言"没有底部入口"，
> 写回去会直接报错。
> ⚠️ 渲染器用 `querySelectorAll` 处理同一 key 下的全部容器（如今每节只有一个）；
> 若改成 `querySelector`，将来任何第二处入口都会整块消失（v0.2.0 开发时踩过）。

### `assets/js/videos.js` 的字段

```js
var VIDEOS = {
  'BV1CAxaeHEeH': {
    title: '《高等数学》全程教学视频 2.0 版（宋浩老师）',
    url:   'https://www.bilibili.com/video/BV1CAxaeHEeH',
    platform: 'bilibili',
    note: '…',
  },
};

var MAP = {
  's1-1': [
    { video: 'BV1CAxaeHEeH', part: 1, partLabel: '映射', verified: false, section: '1.1' },
    { video: 'BV1CAxaeHEeH', part: 2, partLabel: '函数', verified: false, section: '1.1' },
  ],
  'tx1-1': [],        // 显式空数组 = 没有视频，渲染统一占位（不要漏写这个键）
};
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `video` | ✅ | 指向 `VIDEOS` 里的 BV 号 |
| `part` | ✅ | 分 P 号；`null` = 只能按标题找 |
| `partLabel` | ✅ | **该 P 讲什么**——入口要传达的核心信息（区分同一合集的知识点） |
| `verified` | ✅ | 分 P 号是否已人工核实；有 `false` 就在按钮下方显示"待核实"提示 |
| `section` | ✅ | 对应同济章节号（写进按钮 `title`） |
| `duration` | 可选 | 形如 `'12:34'`。**目前全站无此数据，故不写**；写了按钮会自动多一个时长胶囊 |

### 三条硬规则

1. **`MAP` 的键必须与 `site.js` 的 `COURSE` 完全一致**（双向）。
   合集选用（2026-09-16 用户指定）：全站以 **BV1CAxaeHEeH（2.0 版）** 为主来源，
   2024 更新版（BV1Eb411u7Fw）**不再挂入口**（用户指出其部分内容已过时）。
   没有视频的小节**也要写 `[]`**，不要漏键——`check-bilingual.py` B4 会报。
2. **分 P 号必须点开核实后把 `verified` 改成 `true`。**
   交接文档已注明：`BV1CAxaeHEeH` 首位可能有「视频与图书介绍」条目使 P 号整体后移；
   `BV1Eb411u7Fw` **无编号**，只能按分 P 标题匹配。
   规则：**核实到就写 `?p=N`；核实不到就 `part: null` 只挂合集链接，不要猜。**
3. **不显示时长**，除非拿到真实数据。全站只有合集总时长（100:54:03 / 108:09:18），
   单集时长没有可靠来源，编造会误导学生。

### 无视频小节的处理（统一规则）

**显示占位，不隐藏入口。** 渲染器对空数组输出
「本节暂无配套讲解视频」+ 原因说明。
这样不会出现"有的地方空白、有的地方消失"的不一致。

---

## 三·补九　托马斯映射（`.map-note`）

```html
<div class="map-note">
  <div class="mn-head"><span>🧭</span>教材映射 / Textbook Mapping</div>
  <p class="zh">本节对应 <strong>Thomas 14e §X.Y</strong>……</p>
  <p class="en" lang="en">This section corresponds to <strong>Thomas 14e §X.Y</strong>……</p>
</div>
```

**以下小节必须写 `.map-note`**（内容以 `01_交接文档/同济_托马斯_映射表.md` 为准）：

| 节 | 错位情况 |
|---|---|
| `s1-2` 数列的极限 | 托马斯在 **§10.1**，不在极限章 |
| `s1-6` 两个重要极限 | 夹逼在 §2.2，$\lim_{x\to0}\frac{\sin x}{x}$ 在 **§3.5** |
| `s1-7` 无穷小的比较 | 托马斯**无独立节** |
| `s2-4` 参数方程求导 | **§3.7 + §11.2**，跨到第 11 章 |
| `tx1-1`、`tx1-2` | 托马斯独有，同济无对应 |
| 后续各期 | 见主文档 §6 的完整清单 |

> **非错位节也可以写** `.map-note`（说明对应哪几节、范围差异在哪）——只要内容属实。
> `s1-1` 就是这种情形：它对应 Thomas §1.1–1.3，但托马斯把反函数推迟到 §7.1，
> 这个"范围与顺序差异"值得提示。

---

## 三·补十　学时预算

每个小节 `h2` 必须带 `data-hours`，脚本会渲染成标题右侧的学时徽章。

**第 1 期（17 节 / 28 学时）取值必须逐字一致：**

| 章 | 取值 |
|---|---|
| 第 1 章 | `s1-1`=2　`s1-2`=1.5　`s1-3`=2　`s1-4`=1　`s1-5`=1.5　`s1-6`=1.5　`s1-7`=1.5　`s1-8`=1.5　`s1-9`=1.5　`s1-10`=1 |
| 第 2 章 | `s2-1`=2.5　`s2-2`=3　`s2-3`=1.5　`s2-4`=1.5　`s2-5`=1.5 |
| 托马斯补充一 | `tx1-1`=1.5　`tx1-2`=1.5 |

**改学时必须同步改三处**：章节页 `h2` 的 `data-hours`、`site.js` 的 `COURSE[].hours`、本表。

### 内容量与学时的对应

| 学时 | 正文 | 例题 |
|---|---|---|
| 1–1.5 h | 1000–1600 字 | 2 道 |
| 1.5–2 h | 1600–2200 字 | 2–3 道 |
| 2–3 h | 2200–3200 字 | 3 道 |
| 3 h 以上 | 3200 字以上 | 4 道以上 |

> ⚠️ **这里的"字"指中文正文。** 加了等权英文后，页面的**视觉长度约为中文正文的 1.8–2 倍**。
> 1.5 h 的节若中英全写，实际阅读量接近原来的 2 h——所以在**学时徽章**上要按"中文主线"计时，
> 并在 `.exam-box` 或讲评里提示"英文段可先略读，二轮再看"。
> 学时徽章是给学习者的时间预期，写虚了会让人产生"我怎么学得这么慢"的挫败感。

---

## 四、内容深度要求（"能掌握"的标准）

每一节 `h2` 之下，**必须完整包含下列各类内容**，顺序为：

| 顺序 | 内容 | 用什么块 | 要求 |
|---|---|---|---|
| 1 | 前置知识 | `<span class="struct-label">前置知识</span>` | 列出本节真正依赖的已有知识，没有就别硬凑，但要说清"除此之外不需要" |
| 2 | 本节要解决的问题 | `<span class="struct-label">本节要解决的问题</span>` | 一到三句话说清"为什么要有这一节" |
| 3 | 为什么需要这个概念（直觉引入） | `.insight-box` | 用具体例子或几何图像说清动机 |
| 4 | 严格定义 | `.def-box` | 能直接默写；术语用 `.term` 标出 |
| 5 | 定理 / 判定条件 | `.thm-box` | 说明适用条件与直观含义 |
| 6 | 计算步骤 | `.method-box` | 步骤化、可照做；标注每步目的 |
| 7 | 例题 | `.example` | **每节至少 2 道**，解答不跳步 |
| 8 | 易错点 | `.warn-box` | 写学生真实会犯的错，不要写废话 |
| 9 | 考察要点 | `.exam-box` | 说清"考试怎么考、看到题该怎么想" |
| 10 | 工程联系（有机会就写） | `.app-box` | 结合航空发动机 / 结构 / 有限元背景加分；**折叠成 `<details>`** |
| 11 | 知识关联（**不再独立成块**） | ~~`.link-box`~~ | v0.7.2 取消：承上写进「前置知识」、启下写进「本节要解决的问题」末句、横向写在概念旁。判据：正文里 ≥3 处带 §号的回指/前瞻。见 `编排与文风规范.md` §二 |
| 12 | 视频跳转 | `.video-link` | 见 §三·补八；**每节只有一个入口，在讲解区顶部** |
| 13 | 自测 | `.quiz` | 7–9 题，题干中英对照 |

**写作语气**：直接、具体、可执行。避免"众所周知""不难发现"这类空话。
面向的是大一学生，**第一次学高数**，遇到抽象概念要多给一句解释。

**数学正确性**：所有例题必须自己验算一遍。写完在回报里说明验算中发现的不确定处。

### 前向引用：三条处理路径

任何概念**首次出现**时，必须满足下列三条之一：

1. **已讲过** —— 直接用，最好顺手回指（"由 §1-4 定义 1.3"）。
2. **是初高中知识** —— 直接用，并在「前置知识」里声明。
3. **确实必须提前用** —— **就地补「前置提示」**，并链接到正式定义处：

   ```html
   <p class="pre-hint">⚠️ <strong>前置提示</strong>：这里用到了"单调有界准则"。
      它要到 <a href="#s1-6">§1-6</a> 才正式讲，此处先按直观接受。</p>
   ```

**允许的前向引用**（不算违规）：`.link-box` 里的「启下」条目、`（详见 §x-y）`形式的出处标注、
`.exam-box` 里的考点提示。**禁止的**：毫无提示地甩出一个后面才定义的术语或记号。

---

## 五、排版硬指标

- **横向溢出**：320 / 768 / 1280 px 三档下 `documentElement.scrollWidth` 必须等于 `clientWidth`。
- **边距**：正文、公式、选项框到卡片边框的距离 **≥ 8px**。
- 章节页的测验区、讲评区的水平内边距统一为 **1.1rem**。

> ⚠️ **踩过两次的坑——`mjx-container` 的三条规则缺一不可。**
>
> ```css
> mjx-container {
>   display: inline-block;   /* ① */
>   max-width: 100%;
>   overflow-x: auto;
>   overflow-y: hidden;      /* ② */
>   line-height: 0;          /* ③ */
> }
> ```
>
> - **① `display:inline-block` 不能省**：MathJax 生成的 `mjx-container` 默认是 `inline`，
>   而 **`overflow` / `max-width` 对非替换行内元素无效**。少了它，②就是死代码，
>   长公式会把 320px 下的页面顶宽（实测溢出 376px）。
> - **② `overflow-x:auto`**：让过长公式在容器内横向滚动，而不是撑破页面。
> - **③ `line-height:0` 不能省**：这是加 ① 之后**必然引入**的副作用补偿。
>   CSS 规范规定 overflow 非 visible 的 inline-block **基线是底边**；而容器会从
>   body 继承 `line-height:1.78`，行盒比公式高约 10px，这段 leading 把公式基线
>   顶离底边——表现就是**公式在中文里明显偏高**。
>   实测：漏掉这一条时 1344/1870 个行内公式异常。
>
> `tests/check-layout.js` 的「行内公式基线」一节专门守这条线，
> 判据是"容器高 − 内部 svg 高 > 5px"。改完必须跑。

---

## 六、各章小节 id 与标题

**唯一权威来源是 `assets/js/site.js` 的 `COURSE` 数组**，`tests/check-structure.py` 直接从这里解析，
不再维护第二份清单。第 1 期登记如下：

| 章 | `data-page` | `data-chapter` | 小节 id | 学时 |
|---|---|---|---|---|
| 第 1 章 函数与极限 | `ch1` | `1` | `s1-1`…`s1-10` | 15 |
| 第 2 章 导数与微分 | `ch2` | `2` | `s2-1`…`s2-5` | 10 |
| 托马斯补充一 函数与变化率 | `supp1` | `101` | `tx1-1`、`tx1-2` | 3 |

> 后续各期新增章节时：**先改 `site.js` 的 `COURSE`**，再建页面。
> 顺序反了会导致首页进度统计与目录对不上。

---

## 七、上传前自检清单

- [ ] 每个 `h2` 的 `id` 与 `site.js` 的 `COURSE` 逐字一致，无多余空格
- [ ] 每个 `h2` 有 `data-hours`、`data-video-map`（=小节 id）、`data-video-title`
- [ ] 讲解区有**且只有一个** `data-video-pos="top"` 入口容器；**没有** `data-video-pos="bottom"`
- [ ] `assets/js/videos.js` 的 `MAP` 覆盖 `site.js` 的每个小节（无视频写 `[]`）
- [ ] 每个小节有 `.insight-box`、`.def-box`、`.thm-box`、`.method-box`、`.warn-box`、`.exam-box` 各 ≥1
      （**`.link-box` 已取消**，见 `编排与文风规范.md` §二；`.app-box` / `.map-note` 折叠成 `<details>`）
- [ ] ≥2 个 `.example`，其中 ≥1 道 `考研` 级
- [ ] 每个 `.quiz` 的 `data-quiz-id` 全站唯一，且有 `.q-review`
- [ ] 每道 `.q` 都有 `data-level`、`.q-stem-en`、`.q-explain`（含英文要点）
- [ ] `.q-stem` 是 `<div>`，`.q-stem-en` 是它的子元素
- [ ] 填空的 `data-answer` 用 `|` 穷举了写法
- [ ] **整段对照**（§三·补六）：每个 `.bi` 容器里 `.en` 段数与 `.zh` 段数相等（**中文在前**）、无单侧语言；
      独立公式 `$$…$$` 两侧逐字一致；`h2`、`.lede`、`.insight-box`、`.def-box`、`.thm-box`、`.method-box` 都有英文
- [ ] **纯中文块没有英文**（§6.2 豁免清单）：`.warn-box`、`.exam-box`、`.app-box`、`.example`、
      `.q-review`、`.map-note` —— 这些块**不放 `.bi` 容器**，`check-structure.py` 会正向检查不含 `.en`
- [ ] **视频入口**（§三·补八）：每个小节讲解区**只有一个**入口（`data-video-pos="top"`）；
      URL 只在 `assets/js/videos.js` 里出现，正文不硬编码；首页每张章节卡片有 `data-chapter-videos`
- [ ] **引用教材题目时**：每道都自己解过并核实答案，出处与验算结果写进回报（见 §四 版权口径）
- [ ] 全文没有未转义的 `<` `>` 出现在 `$...$` 里（**英文段落里出现 `<` 的概率更高**）
- [ ] 内联 SVG 图内没有 `$…$` 数学、没有 Markdown `**加粗**`（`check-html.py`「内容卫生」会拦）
- [ ] 所有例题都验算过，并在回报里说明不确定处
- [ ] 视频分 P 号已点开核实（核实不到就只挂合集链接）

跑一遍就能验完上面大半：

```bash
python3 tests/check-structure.py    # 结构、双语覆盖、视频登记、难度分布
python3 tests/check-bilingual.py    # 整段对照（段数/单侧/公式/数字）+ 视频入口 + 点击深度
python3 tests/check-html.py         # 标签配平、内部链接、锚点
node tests/quiz.test.js             # 引擎行为（含讲评块位置回归）
node tests/review.test.js           # 讲评报告 / 订正 / 可视化引擎
node tests/site.test.js             # 站点集成（首页、目录、进度、勾选）
node tests/check-layout.js          # 真实浏览器：三档宽度溢出 + 公式基线
```

> **运行环境说明**：本机**没有 `bun`**，用 `node` 直接跑 `.test.js` 即可（Node ≥ 20，
> 实测 24.21.0）。首次需在 `tests/` 下 `npm install`（`happy-dom` + `puppeteer-core`）。
> 若 `~/.npm` 缓存权限有问题，加 `--cache /tmp/npmcache-dsh`。
