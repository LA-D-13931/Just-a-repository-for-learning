# 视觉规范（阅读舒适度）

> 起因：用户反馈 **"读这种有背景色的字体有点难受"**（附图是 `定义 1.2 函数` 那个绿色卡片）。
> 本文记录**问题诊断的量化依据**、**4 个候选方案与取舍**、以及**最终采用的口径**。
> 改这一块之前请先读本文，别凭感觉调。

---

## 一、问题诊断（不是感觉，是数字）

用生成器里的颜色数学量了一下"内容块底色"和"纸面"的明度差（WCAG 相对亮度）：

| token | 亮色值 | 与纸面 `--bg` 的明度差 |
|---|---|---|
| `--bg`（纸面） | `#f4f5f6` | — |
| `--bg-elev`（卡片） | `#fcfcfd` | 0.062 |
| `--def-soft`（原定义块底） | `#b4d6cf` | **0.289** |
| `--method-soft` | `#c0d4de` | 0.276 |
| `--app-soft` | `#ccd6e4` | 0.247 |
| `--link-soft` | `#e4d0e1` | 0.242 |
| `--warn-soft` | `#e8d2d0` | 0.234 |
| `--thm-soft` | `#e2dbeb` | 0.184 |
| `--exam-soft` | `#e9dfda` | 0.160 |

**结论**：`--def-soft` 的明度差是卡片的 **4.6 倍**——那根本不是"浅色块"，是一整片中绿底。
长的中文段落压在上面，眼睛要同时处理"字形"和"大面积色块"，所以累。

**第二个噪音源**：`.zh` 与 `.en` **每一段都带一条 3px 左边竖线**。
一屏十几段就是十几条竖线，加上块本身的左侧色条 + 描边，构成"框套框套线"。

**第三个**：术语用 `1.5px dotted` 点线下划线。中文密度高，点线看起来像"满页都是链接"。

---

## 二、4 个候选方案（都有实物截图）

截图在桌面：`视觉方案_A/B/C/D_*.png`（用真实内容块 + 真实 MathJax 渲染出来的）。

| 方案 | 做法 | 优点 | 代价 |
|---|---|---|---|
| A · 纸面正文 + 色条识别 | 内容块背景改纸面 `--bg-elev`；语义只靠**左侧 4px 色条 + 图标 + 行内小色带**；去掉逐段竖线；英文段组给一层极浅**中性**底 | 正文全部落在中性纸面上，最耐读；语义识别没丢 | 色块"块感"变弱，分区更像"条目"而不是"卡片" |
| **B · 极浅色块** | 保留色块，但把 `-soft` 与纸面混合到约 34% | 仍能一眼看出"这是定义块" | 仍有大面积色块，只是浅了；长文依旧不如 A 舒服 |
| **C · 只给标题上色**（✅ **用户选定，已采用**） | 正文纸面，**标题做成整条圆角色带**（比 A 的行内细带更重） | 结构感最强、最"教科书" | 标题带面积更大，页面上"色块条数"变多 |
| **D · 最小改动** | 保留原色块（压到 62%）+ 去掉逐段竖线 + 行高 1.78→1.86 | 改动最小、最接近原设计 | 色块明度差仍有 0.18 左右，治标 |

> A 与 C 的差别只有"标题色带的轻重"：A 是**行内小色带**，C 是**整条标题带**。
> **用户 2026-09-16 选定 C**，实现即为 `.box-title { display:block; padding:.42rem .7rem;
> border-radius:6px; }`（`视觉方案_C_只给标题上色.png` 是当时的选型图，已按"清垃圾"要求删除）。

---

## 三、已采用口径（方案 C，v0.6.0 起）

### 硬规则

1. **正文不铺彩色。** 内容块、视频框、教材映射框的正文背景一律 `--bg-elev`（纸面）。
2. **语义只用三处表达**：左侧 4px 色条 · 图标 · **整条标题色带**（`--{token}-title-bg`）。
   标题带是 `display:block` 的满宽圆角条（方案 C 与 A 的唯一差别）。
3. **标题色带的文字色必须对"色带本身"反解**，不是对 `-soft` 反解。
   生成器现在提供成对的 `--{tok}-title-bg` 与 `--{tok}-on-soft`，断言 ≥ 7:1。
   （踩过：正文改纸面后标题色带变了，旧文字色在暗色下掉到 **4.1:1**，被 `check-contrast.js` 抓到。）
4. **不做逐段装饰。** 中文段不加竖线；**英文段组**用一层极浅中性底 `--bg-sunken` 表达
   "这里是对照区"，连续几段要看起来像**一条**连续面板（首尾圆角、中间无缝）。
5. **重点文字加下划线**（用户 2026-09-16 要求："文本重点加下划线"）：
   `strong / b / .term / .term-en` 一律 1px 实线、`text-underline-offset: 2px`、
   下划线色 = 文字色 45~55% 透明。**不用点线**（中文密度高点线会像"满页链接"）；
   **标题内的 strong 不加线**（标题本身已是重点，再加线会糊）。
6. 所有涉及文字的改动，交付前必须跑 `node tests/check-contrast.js`
   （判据：任何文字 ≥ 4.5:1；正文/链接/按钮 ≥ 7:1）。

### 关于"分区可辨"与"阅读舒适"的取舍

主题任务里曾要求"相邻功能区色差要可辨"。方案 A 把"可辨"从**底色**转移到了
**色条 + 图标 + 标题色带**上——三处都是**色相差**（不是同色系深浅），
所以识别性满足要求，而正文区的视觉负担降到最低。

---

## 四、改这块时容易踩的坑（都踩过了）

| 坑 | 后果 | 预防 |
|---|---|---|
| 在文件**后面**重复定义 `.en` / `.zh` | 后面那条覆盖前面，英文段被拆成一个个独立灰卡片、中间有缝（v0.5.0 截图可见） | 同类选择器只允许出现一次；改完 `grep -c '^\.en {'` 应为 1 |
| 替换 CSS 时用**已被 token 化前的十六进制色值**当锚点 | 替换静默不生效（视频框/映射框仍是彩底） | 替换后回查目标属性；用 token 名当锚点 |
| 批量替换 `#fff` 这类**短色值** | `#fffbf2` 被 `#fff` 先匹配，变成 `var(--bg-elev)bf2`（非法值，整条声明失效） | 长色值优先替换；改完全盘搜 `var(--…)<十六进制残片>` |
| 标题文字色沿用"对 `-soft` 反解"的结果 | 底色一改就掉到 4.1:1 | 文字色与底色必须**成对**生成 |
